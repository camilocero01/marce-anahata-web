---
import { Image } from 'astro:assets';
import marceImg from '../assets/sobre-mi/sobre-mi.webp';
import '../styles/global.css';

interface ServiceLink {
  title: string;
  description: string;
  url: string;
  icon: string;
  gradient: string;
}

const services: ServiceLink[] = [
  {
    title: 'Rituales Personalizados',
    description: 'Ceremonias sagradas diseñadas para ti',
    url: '/rituales',
    icon: '✨',
    gradient: 'from-[#1a7f7e] to-[#4a4763]'
  },
  {
    title: 'Terapia de Sonido',
    description: 'Sanación a través de vibraciones',
    url: '/terapia-sonido',
    icon: '🔊',
    gradient: 'from-[#4a4763] to-[#1a3a52]'
  },
  {
    title: 'Clases de Yoga',
    description: 'Conexión cuerpo-mente-espíritu',
    url: '/clases',
    icon: '🧘',
    gradient: 'from-[#1a3a52] to-[#1a7f7e]'
  },
  {
    title: 'Bienestar Corporativo',
    description: 'Paz y balance para tu equipo',
    url: '/bienestar-corporativo',
    icon: '🌿',
    gradient: 'from-[#4a4763] to-[#1a3a52]'
  },
  {
    title: 'Conoce mi Historia',
    description: 'Descubre mi camino en el bienestar',
    url: '/sobre-mi',
    icon: '💫',
    gradient: 'from-[#1a7f7e] to-[#1a3a52]'
  }
];

const socialLinks = [
  {
    name: 'Instagram',
    url: 'https://instagram.com/tu_usuario',
    icon: '📷'
  },
  {
    name: 'WhatsApp',
    url: 'https://wa.me/57123456789',
    icon: '💬'
  },
  {
    name: 'Contacto',
    url: '/contacto',
    icon: '📧'
  }
];
---

<div class="min-h-screen bg-gradient-to-b from-[#0f2438] via-[#1a3a52] to-[#0f2438] flex items-center justify-center py-8 px-4">
  <div class="w-full max-w-md">
      <!-- Header Section -->
      <div class="text-center mb-8">
        <!-- Profile Image -->
        <div class="mb-6 flex justify-center">
          <div class="relative w-24 h-24 md:w-32 md:h-32">
            <div class="absolute inset-0 bg-gradient-to-r from-[#6ed8d9] to-[#4a9fa0] rounded-full p-1">
              <Image
                src={marceImg}
                alt="Marce Anahata"
                width={128}
                height={128}
                class="w-full h-full rounded-full object-cover bg-white"
                densities={[1.5, 2]}
              />
            </div>
          </div>
        </div>

        <!-- Title and Description -->
        <h1 class="text-4xl md:text-5xl font-serif text-white mb-2 font-bold">Marce Anahata</h1>
        <p class="text-[#6ed8d9] text-lg font-semibold mb-3">Facilitadora de Bienestar</p>
        <p class="text-white text-base md:text-lg max-w-xs mx-auto leading-relaxed font-medium">
          Transformación energética a través de rituales, terapia de sonido y yoga
        </p>
      </div>

      <!-- Service Links -->
      <div class="space-y-4 mb-8">
        {services.map((service) => (
          <a
            href={service.url}
            class={`block group relative overflow-hidden rounded-2xl p-6 transition-all duration-300 transform hover:scale-105 hover:shadow-2xl`}
          >
            <!-- Gradient Background -->
            <div class={`absolute inset-0 bg-gradient-to-r ${service.gradient} opacity-90 group-hover:opacity-100 transition-opacity`}></div>
            
            <!-- Animated Border on Hover -->
            <div class="absolute inset-0 border-2 border-white opacity-0 group-hover:opacity-30 rounded-2xl transition-opacity"></div>
            
            <!-- Content -->
            <div class="relative z-10">
              <div class="flex items-start justify-between mb-2">
                <div>
                  <h3 class="text-xl font-serif text-white mb-1">{service.title}</h3>
                  <p class="text-white/90 text-sm">{service.description}</p>
                </div>
                <span class="text-2xl ml-2 flex-shrink-0">{service.icon}</span>
              </div>
            </div>
            
            <!-- Arrow indicator -->
            <div class="absolute right-6 top-1/2 -translate-y-1/2 text-white opacity-0 group-hover:opacity-100 group-hover:translate-x-1 transition-all transform">
              →
            </div>
          </a>
        ))}
      </div>

      <!-- Social Links -->
      <div class="bg-white/15 backdrop-blur-sm rounded-2xl p-6 mb-6 border-2 border-[#6ed8d9]">
        <p class="text-white font-semibold text-center mb-4">Conecta conmigo</p>
        <div class="grid grid-cols-3 gap-3">
          {socialLinks.map((social) => (
            <a
              href={social.url}
              title={social.name}
              target={social.url.startsWith('http') ? '_blank' : undefined}
              rel={social.url.startsWith('http') ? 'noopener noreferrer' : undefined}
              class="flex flex-col items-center justify-center p-4 rounded-xl bg-[#1a7f7e]/60 hover:bg-[#6ed8d9] text-white hover:text-[#0f2438] transition-all duration-300 group border border-[#6ed8d9]/30 hover:border-[#6ed8d9]"
            >
              <span class="text-2xl mb-1 group-hover:scale-110 transition-transform">{social.icon}</span>
              <span class="text-xs font-semibold text-center leading-tight">{social.name}</span>
            </a>
          ))}
        </div>
      </div>

      <!-- Footer CTA -->
      <div class="text-center text-white/60 text-xs font-light">
        <p class="mb-4">Elige tu camino hacia la transformación</p>
        <div class="flex justify-center gap-2">
          <div class="w-1 h-1 rounded-full bg-[#6ed8d9]"></div>
          <div class="w-1 h-1 rounded-full bg-[#6ed8d9]"></div>
          <div class="w-1 h-1 rounded-full bg-[#6ed8d9]"></div>
        </div>
      </div>
    </div>
  </div>

<style>
  @supports (backdrop-filter: blur(1px)) {
    .backdrop-blur-sm {
      backdrop-filter: blur(12px);
    }
  }

  /* Smooth scroll behavior */
  html {
    scroll-behavior: smooth;
  }

  /* Animation for profile image */
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  img[src*="sobre-mi"] {
    animation: float 3s ease-in-out infinite;
  }
</style>
